autoencoder.py

# Node
node_modules/
sim/node_modules/

# Local dependency workaround (fetched from CDN on network-restricted hosts;
# real installs pull three from npm)
sim/vendor/

# Generated datasets — regenerable from seed, never committed. Anchored to the
# repo root so it doesn't also swallow model/data/ (the dataloader source).
/data/

# Logs
*.log
npm-debug.log*

# Python
__pycache__/
*.pyc
