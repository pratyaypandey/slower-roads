{
  "name": "slower-roads-sim",
  "version": "0.0.1",
  "type": "module",
  "description": "Deterministic driving sim + oracle (M0). See ../plans/SIM.md",
  "scripts": {
    "test": "node test/core.smoke.mjs"
  },
  "//optionalDependencies": "playwright is only for headless/generate_pixels.mjs (WebGL frame capture); the core sim + headless/generate.mjs need nothing.",
  "optionalDependencies": {
    "playwright": "^1.44.0"
  }
}
