{
  "name": "slower-roads-sim",
  "version": "0.0.1",
  "type": "module",
  "description": "Deterministic driving sim + oracle (M0). See ../plans/SIM.md",
  "scripts": {
    "test": "node test/core.smoke.mjs"
  },
  "//optionalDependencies": "playwright + gifenc are only for the WebGL capture scripts (generate_pixels.mjs, render_dream.mjs); the core sim + headless/generate.mjs need nothing.",
  "optionalDependencies": {
    "playwright": "^1.44.0",
    "gifenc": "^1.0.3"
  }
}
